#!/bin/sh /etc/rc.common
START=16
STOP=12
# The command(s) you want to run on start
start() {        
    sleep 3
    /usr/sbin/vpn-select
}
# The command(s) you want to run on stop
stop() {          
        echo Nothing to do
}
